// lib/services/leaveService.ts
//
// ทางเข้าเดิม — ของจริงแตกเป็นโมดูลอยู่ใน ./leave/ แล้ว
// เก็บไฟล์นี้ไว้เพื่อให้ไฟล์ที่ import ทางเดิมไม่ต้องแก้
// ของเดิมที่ใช้ Firestore ลบทิ้งแล้ว — ย้อนดูได้ใน git history

export * from './leave'
