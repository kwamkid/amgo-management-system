-- 🚨 กู้คืน users.business_unit_id ชั่วคราว
--
-- ── สิ่งที่เกิดขึ้น ────────────────────────────────────────────────────
-- ลบคอลัมน์นี้ทิ้งตอนย้ายไป บริษัท+หน้าที่ โดยลืมไปว่า "ของจริงยังรันโค้ดเก่า"
-- ที่ยังเขียนคอลัมน์นี้อยู่  ผลคือ HR กดบันทึกเงินเดือนแล้วไม่ผ่าน
-- (postgres log: column users.business_unit_id does not exist — 9 ครั้ง)
--
-- บทเรียน: ลบคอลัมน์ได้ก็ต่อเมื่อโค้ดที่ deploy อยู่เลิกใช้แล้วเท่านั้น
-- ไม่ใช่ตอนที่โค้ดในเครื่องเลิกใช้
--
-- ── ทำไมไม่ผูก FK กลับ ────────────────────────────────────────────────
-- ตาราง business_units ถูกลบไปแล้ว คอลัมน์นี้จึงเป็นแค่ที่รองรับค่าที่โค้ดเก่า
-- เขียนมา ไม่มีใครอ่าน  ลบทิ้งจริงได้หลัง deploy เวอร์ชันใหม่แล้ว

alter table public.users
  add column if not exists business_unit_id uuid;

comment on column public.users.business_unit_id is
  '🗑️ เลิกใช้แล้ว — เหลือไว้ให้โค้ดเวอร์ชันเก่าบนของจริงเขียนได้ ลบได้หลัง deploy';
