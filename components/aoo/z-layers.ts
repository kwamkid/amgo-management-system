// components/aoo/z-layers.ts
//
// ลำดับการซ้อนทับของทุกอย่างที่ลอยอยู่เหนือหน้าจอ
//
// ── ทำไมต้องรวมไว้ที่เดียว ────────────────────────────────────────────
// ก่อนหน้านี้แต่ละคอมโพเนนต์ตั้งเลขเอง แล้วเลขไม่สัมพันธ์กัน:
//   modal 200 · action-menu 300 · date-range-picker 1000
//   select-menu 9999 · time-picker 9999 · toast 9999 · date-picker 70
//
// ผลคือ DatePicker (70) เปิดในกล่อง Modal (200) แล้วปฏิทินไปอยู่ "หลัง" กล่อง
// — เจอตอนกดเลือกวันสุดท้ายที่ทำงานในหน้าจบการเป็นพนักงาน
//
// เลขพวกนี้เดาเองไม่ได้ ต้องดูทั้งหมดพร้อมกัน จึงต้องอยู่ไฟล์เดียว
// ห้ามเขียน zIndex เป็นตัวเลขดิบในคอมโพเนนต์อีก ให้ import จากที่นี่

export const Z = {
  /** เมนู/ปฏิทินที่กางในหน้าปกติ */
  dropdown: 300,

  /** กล่องโต้ตอบ — ทับเนื้อหาหน้า แต่ต้องอยู่ใต้เมนูที่กางในกล่องเอง */
  modal: 500,

  /**
   * เมนู ปฏิทิน และตัวเลือกเวลา ที่กางอยู่ "ใน" กล่องโต้ตอบ
   * ต้องสูงกว่า modal ไม่งั้นตัวเลือกไปอยู่หลังกล่อง
   */
  dropdownInModal: 700,

  /** คำอธิบายสั้น ๆ ตอนเอาเมาส์ชี้ */
  tooltip: 900,

  /** ข้อความแจ้งผล — ต้องเห็นเสมอ ไม่ว่าจะเปิดอะไรค้างอยู่ */
  toast: 1000,
} as const
